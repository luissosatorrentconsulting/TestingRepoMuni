[phases.setup]
nixPkgs = ["php82", "php82Packages.intl", "php82Packages.gd", "icu", "nodejs_20"]

[phases.build]
cmds = ["npm install && npm run build", "composer install --no-dev --optimize-autoloader"]

[phases.start]
cmd = "php -d extension=intl artisan serve --host 0.0.0.0 --port $PORT"